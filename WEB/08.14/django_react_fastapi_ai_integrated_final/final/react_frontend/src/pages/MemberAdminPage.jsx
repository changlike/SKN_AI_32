import { useEffect, useState } from 'react';
import { apiFetch } from '../api/client';
import Alert from '../components/Alert';

export default function MemberAdminPage(){const[members,setMembers]=useState([]);const[msg,setMsg]=useState('');const load=()=>apiFetch('/api/members/admin-list/').then(d=>setMembers(d.members));useEffect(()=>{load();},[]);const toggle=async(id)=>{const d=await apiFetch(`/api/members/${id}/toggle-active/`,{method:'POST'});setMsg(d.message);load();};return <div className="card shadow-sm"><div className="card-body"><h2>회원 관리</h2><Alert type="success">{msg}</Alert><div className="table-responsive"><table className="table table-hover align-middle"><thead><tr><th>ID</th><th>아이디</th><th>이름</th><th>이메일</th><th>가입방식</th><th>로그인</th><th>관리</th></tr></thead><tbody>{members.map(m=><tr key={m.id}><td>{m.id}</td><td>{m.username}</td><td>{m.display_name}</td><td>{m.email}</td><td>{m.sign_type}</td><td><span className={`badge ${m.is_active?'text-bg-success':'text-bg-danger'}`}>{m.is_active?'허용':'제한'}</span></td><td><button className="btn btn-sm btn-outline-primary" onClick={()=>toggle(m.id)}>{m.is_active?'로그인 제한':'로그인 허용'}</button></td></tr>)}</tbody></table></div></div></div>}
