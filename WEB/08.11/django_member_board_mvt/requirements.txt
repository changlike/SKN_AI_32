# Django 5.2 LTS 계열을 사용하여 Python 3.11 기반 수업 환경에서 안정적으로 실행합니다.
Django>=5.2,<5.3
# Django가 MySQL과 통신할 때 사용하는 공식적으로 널리 사용되는 DB API 드라이버입니다.
mysqlclient>=2.2.8,<3.0
# 회원 프로필 이미지 업로드를 ImageField로 처리하기 위해 사용합니다.
Pillow>=11.0,<13.0
# .env 파일에서 MySQL 접속 정보와 SECRET_KEY를 읽기 위해 사용합니다.
python-dotenv>=1.0,<2.0
